1. I created a window forms application for testing and running the code 
2. On button click event, calling the static ParseInput.ParseInputData to parse the data
3. same method will provide the response back in the xpected format.